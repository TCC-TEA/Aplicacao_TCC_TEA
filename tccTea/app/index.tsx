import { SafeAreaView, Text } from "react-native";

const Screen = () => {
    return (
        <SafeAreaView className="flex-1 items-center justify-center">
            <Text className="font-semibold text-3xl">TCC TEA</Text>
        </SafeAreaView>
    );
}

export default Screen;
